// Re-export the shared admin toggle so existing imports in this folder
// keep working while the visual primitive lives in @/Components/Admin/Shared.
export { default } from '@/Components/Admin/Shared/Toggle';
